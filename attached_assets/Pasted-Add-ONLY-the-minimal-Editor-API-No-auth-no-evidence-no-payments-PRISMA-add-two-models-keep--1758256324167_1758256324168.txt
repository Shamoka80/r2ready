Add ONLY the minimal Editor API. No auth, no evidence, no payments.

PRISMA (add two models; keep existing ones unchanged)
- model Assessment {
    id        String   @id @default(cuid())
    stdId     String
    createdAt DateTime @default(now())
    standard  StandardVersion @relation(fields: [stdId], references: [id])
    answers   Answer[]
  }
- model Answer {
    id           String   @id @default(cuid())
    assessmentId String
    questionId   String
    value        Json
    updatedAt    DateTime @updatedAt
    assessment   Assessment @relation(fields: [assessmentId], references: [id])
    question     Question   @relation(fields: [questionId], references: [id])
    @@unique([assessmentId, questionId])
  }
- Create a migration named "add-assessment-and-answers".

SERVER (use the existing server layout)
- New router file at server/routes/assessments.ts (or server/src/routes/assessments.ts if that’s where /api/health lives).
- Mount it under /api in the server entrypoint.

Endpoints (Zod-validated JSON):
1) POST /api/assessments
   - Body: optional { stdCode: string } default "R2V3_1"
   - Creates an assessment linked to the StandardVersion.
   - Returns { id, stdId, createdAt }.

2) GET /api/assessments/:id/questions
   - Returns questions grouped by clause for that assessment’s standard:
     {
       assessmentId, standardCode,
       groups: [
         { clauseRef, clauseTitle, questions: [
             { id, questionId, text, required, evidenceRequired, responseType, appendix,
               category, category_code, category_name }
           ]
         }
       ],
       totalQuestions, requiredCount
     }

3) POST /api/answers/:assessmentId/batch
   - Body: { answers: [{ questionId: string, value: any }] } (1–100 items)
   - Upsert per (assessmentId, questionId); returns { upserted: n }.

Acceptance:
- Prisma migration applies.
- Endpoints respond with JSON as specified.
- No unrelated files changed; no new deps.
Return the list of changed files and whether you used server/routes or server/src/routes.
