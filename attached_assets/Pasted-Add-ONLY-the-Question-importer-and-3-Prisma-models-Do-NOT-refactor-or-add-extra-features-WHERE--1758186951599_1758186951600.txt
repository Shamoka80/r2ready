Add ONLY the Question importer and 3 Prisma models. Do NOT refactor or add extra features.

WHERE TO EDIT
- Use the existing server TypeScript directory (where /api/health is defined). If the entrypoint is server/index.ts, create a subfolder server/routes for routers and server/tools for CLI. If the entrypoint is server/src/index.ts, create server/src/routes and server/src/tools instead. Choose whichever matches the current repo.

PRISMA (models + migration)
- Add models: 
  StandardVersion { id cuid, code string @unique, name string, isActive bool @default(true) }
  Clause { id cuid, ref string @unique, title string, stdId -> StandardVersion }
  Question { id cuid, questionId string @unique, clauseId -> Clause, text string,
             responseType string, required bool @default(false), evidenceRequired bool @default(false),
             appendix string?, weight float @default(1), helpText string? }
- Create a migration named "init-questions".

IMPORTER (route + CLI)
- Dependencies: csv-parse@5 (install if missing).
- CLI file: <server>/tools/import-questions.ts (path depends on layout as noted above).
  Behavior: delimiter auto-detect (',' ';' '\t' '|'), header synonyms:
    question_id|id|qid|q_id, clause_ref|clause|requirement|citation, 
    text|question|prompt, response_type|type, required|is_required|mandatory,
    evidence_required|requires_evidence, appendix|scope, weight|score_weight, help_text|guidance
  Upsert StandardVersion { code: "R2V3_1", name: "R2 v3.1" }; upsert Clause by ref; upsert Question by questionId.
  Print coverage counts for CR1..CR10 and APP-A..G + missing/duplicate counts. End with “Total imported: N”.

- Router file: <server>/routes/adminImport.ts
  POST /api/admin/import-questions (multipart file upload) → run same logic
  GET  /api/admin/import-questions/coverage → return JSON coverage

- Wire the router in the server entrypoint under prefix /api/admin.

ACCEPTANCE
- prisma migrate dev completes.
- CLI import prints a coverage table and “Total imported: N”.
- GET /api/admin/import-questions/coverage returns JSON with keys CR1..CR10 and APP-A..APP-G.
- Only these files changed: prisma/schema.prisma (+ migration), server/.../tools/import-questions.ts, server/.../routes/adminImport.ts, server entrypoint (router mount), server/package.json (add script: "import:questions": "ts-node <tools path>/import-questions.ts")
Return the list of changed files and which layout you used (server/… or server/src/…).
